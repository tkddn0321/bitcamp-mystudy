<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardDao">

    <select id="list" resultType="bitcamp.myapp.vo.Board">
        select
        board_id as no,
        name,
        email
        from
        myapp_boards
        order by
        board_id asc
    </select>

    <!-- parameterType으로 변경 -->
    <insert id="insert" parameterType="bitcamp.myapp.vo.Board">
        insert into myapp_boards(title, content, user_id) values (?, ?, ?)
    </insert>

    <select id="findBy" resultType="bitcamp.myapp.vo.Board" parameterType="int">
        select
        board_id as no,
        name,
        email,
        tel
        from
        myapp_boards
        where
        board_id=#{okok}
    </select>

    <update id="update" parameterType="bitcamp.myapp.vo.Board">
        update
        myapp_boards set
        name=#{name},
        email=#{email},
        pwd=sha1(#{password}),
        tel=#{tel}
        where
        board_id=#{no}
    </update>

    <delete id="delete" parameterType="int">
        delete from myapp_boards
        where
        board_id=#{ok}
    </delete>

</mapper>
